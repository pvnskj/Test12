name: Production deployment disabled pending approval

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment guard
        run: |
          echo "Production deployment is intentionally disabled in the redesign."
          echo "Replace this guard only after explicit approval to deploy."
